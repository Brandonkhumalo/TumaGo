# Production environment — baked into Next.js at build time
# Client-side: browser calls /api/v1/* which nginx routes to the Go gateway
NEXT_PUBLIC_API_URL=/api/v1
# Server-side: Next.js rewrites proxy to the Go gateway via Docker network
BACKEND_URL=http://gateway:8080
