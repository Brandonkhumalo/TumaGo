# TEMPLATE ONLY — do not commit real values.
# Create the real secret with:
#   kubectl create secret generic tumago-secrets \
#     --from-literal=SECRET_KEY="..." \
#     --from-literal=DATABASE_URL="..." \
#     --from-literal=GOOGLE_MAPS_API_KEY="..." \
#     --from-literal=POSTGRESQL_USERNAME="postgres" \
#     --from-literal=POSTGRESQL_PASSWORD="..." \
#     -n tumago
apiVersion: v1
kind: Secret
metadata:
  name: tumago-secrets
  namespace: tumago
type: Opaque
stringData:
  SECRET_KEY: "REPLACE_ME"
  DATABASE_URL: "postgresql://postgres:REPLACE_ME@pgbouncer-service:5432/postgres"
  GOOGLE_MAPS_API_KEY: "REPLACE_ME"
  POSTGRESQL_USERNAME: "postgres"
  POSTGRESQL_PASSWORD: "REPLACE_ME"
